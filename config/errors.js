const ERROR_INIT_MSG = "No existe el archivo de configuración de entorno, proceda a ejecutar npm run init para generarlo";
const ERROR_KEYS_MSG = "No existe la carpeta de las claves para poder generar los tokens de las APIs";
const ERROR_PVTKEY_MSG = "No existe el archivo de llave privada";
const ERROR_PBLKEY_MSG = "No existe el archivo de llave pública";
const ERROR_DB_MSG = "Hubo un error al conectarse a la Base de Datos";

module.exports = {
	ERROR_INIT_MSG,
	ERROR_KEYS_MSG,
	ERROR_DB_MSG,
	ERROR_PVTKEY_MSG,
	ERROR_PBLKEY_MSG,
};
